---
title: "Poland"
description: "What Coverbase asks a supplier in Poland for, and which of those rules block a submission."
hidden: true
---

{/* Generated by common/scripts/kys/generate_country_docs.py in the irm repo. */}
{/* Edit the country configurations and regenerate; do not edit this file. */}

A supplier whose bank account is held in Poland sees the fields below. The country is chosen on the form, and changing it changes the fields, so a supplier is never asked for a detail their country does not use.

## Bank details

| Field | Requirement | Format | Notes |
| --- | --- | --- | --- |
| Bank name | Optional | up to 60 characters |  |
| Bank code | Optional | 8 characters, digits only |  |
| Branch code | Optional | 8 characters, digits only |  |
| Account number | Required | 16 characters, digits only |  |
| Check digit | Optional | no length limit, digits only |  |

### IBAN

An IBAN is required for Poland. It is 28 characters and is checked against the ISO 7064 MOD-97 algorithm, which catches a mistyped or transposed digit before the details reach your finance system.

## Tax and company registrations

| Registration | Requirement | Format | Notes |
| --- | --- | --- | --- |
| NIP | Required | 10 characters, digits only, NIP check digit | 10 digits. |

After a registration is saved, Coverbase checks it against the EU VIES service in the background. That check never blocks the supplier: it annotates the record once it completes, and a reviewer can retry it.

## Country rules

- The bank code and branch code must match each other.

## Where these rules come from

Oracle Fusion Cash Management 26c PL validations.

The rules on this page are confirmed against a primary source, so they are enforced strictly.

Configuration version `2026.08.1`. Your organization can override any of these rules for its own suppliers.
