---
title: "Negotiation rounds"
description: "Upload the marked-up contract a vendor sends back, see what they changed, which of your asks they accepted, and what they edited without being asked, without starting the clause review over."
icon: "arrow-right-arrow-left"
---

import AgentDirective from "/snippets/agent-directive.mdx";

<AgentDirective />

<Info>
  This guide is part of the [User Guides](/user-guides/overview) collection. It picks up where the [Contract Guardian guide](/user-guides/contract-guardian) ends: you have run a clause review, decided the flagged clauses, and sent the vendor a redline. For how those asks are written and sent, read that guide first.
</Info>

<Warning>
  Negotiation rounds are in design and not yet available in your workspace. This guide describes the flow as it is being built, so the screens below are mockups. Your Coverbase contact can tell you where the feature sits in the roadmap.
</Warning>

A contract review does not end when you send the redline. The vendor answers, usually weeks later, usually with a marked-up document, and you have three questions:

- **What did they change?**
- **Did they give me what I asked for?**
- **What is still open?**

A round answers all three against the review you already ran. You do not start a second review, and you do not lose the verdicts you recorded the first time.

## What a round is

One exchange: you send asks, the vendor returns a document, Coverbase reads it against what you asked for.

| | |
| --- | --- |
| **Round 1** | Your first clause review. You decide the flagged clauses and send a redline. |
| **Round 2** | You upload what the vendor sent back. Coverbase compares it to round 1 and tells you what moved. |
| **Round 3+** | The same again. Contracts that go three or four rounds are normal, and each one only asks about what changed. |

Your decisions carry. A clause whose language the vendor did not touch keeps the verdict you gave it, including an accepted risk and its expiry date. You are never asked about the same unchanged clause twice.

## Before you start

<Steps>
  <Step title="Have a completed round">
    You need a clause review on this contract with at least one ask sent to the vendor. If nothing has been sent yet, there is nothing to compare against, so run the review first.
  </Step>
  <Step title="Have the vendor's document">
    A PDF or Word file of the contract as the vendor returned it. Their cover email or response letter is useful too; you can upload both.
  </Step>
  <Step title="Check your permissions">
    Starting a round needs the same contract-update permission as starting a review.
  </Step>
</Steps>

<Note>
  You do not have to finish reviewing round 1 before starting round 2. If the vendor comes back early, upload their document. Clauses you had not decided yet stay undecided and carry into the new round.
</Note>

## Step 1: Upload what the vendor sent back

While a round is out with the vendor, the review page says so, and the upload sits on that same banner. **Upload vendor response** is also in the review page header and on the contract's **Review** tab, so you do not have to remember which screen you were on.

<Frame caption="A sent round, waiting on the vendor. The asks you sent are listed underneath so you know what you are waiting for.">
  <img src="/images/user-guides/round-waiting-on-vendor.png" alt="Review page banner reading round 1 sent, 8 asks outstanding, with an upload vendor response button and the list of asks below" />
</Frame>

Drop the files in. Before anything changes, Coverbase tells you what it read each file as, and why.

<Frame caption="Each uploaded file, with what it replaces and the evidence for that reading. Change any of it before you continue.">
  <img src="/images/user-guides/round-upload-response.png" alt="Upload dialog showing a redlined MSA marked as replacing the original, and a response letter marked as an addition" />
</Frame>

Two outcomes, and the difference matters:

- **Replaces** — this is a new version of a document already on the contract. The old version stops being reviewed; the new one takes its place.
- **Adds** — this is a new document alongside the others, such as a cover letter or a side agreement.

Check this before continuing. Getting it wrong is the one mistake that costs you: a replacement read as an addition leaves both versions in scope, and the review then reports clauses from language nobody is bound by any more. When Coverbase is not confident, it asks rather than guessing.

Then press **Compare against round 1**.

## Step 2: See what changed

The round opens on its answer, before you review a single clause.

<Frame caption="The round summary. Four buckets, each stating what it means for you, and the riskiest one first.">
  <img src="/images/user-guides/round-what-changed.png" alt="Round summary reading the vendor answered 4 of your 8 asks, with buckets for changed without being asked, countered, not addressed, and accepted" />
</Frame>

| Bucket | What it means | What to do |
| --- | --- | --- |
| **Changed without being asked** | The vendor edited a clause you never raised, including ones you accepted in round 1. | Read every one. This is the bucket that catches quiet edits. |
| **Countered** | They moved, but the language still falls short of the tier you asked for. | Decide whether the counter is good enough. |
| **Not addressed** | You asked; nothing changed. | Decide whether to press again, accept the risk, or drop it. |
| **Accepted** | The language now meets the tier you asked for. | Nothing, unless you disagree with the read. |

**Changed without being asked is listed first on purpose.** An edit in a clause you already signed off is the thing a returned document is most likely to hide, and it is the reason to read a marked-up contract with a tool rather than by eye.

Each bucket filters the list beneath it, so you can work one at a time.

## Step 3: Review what needs you

The queue holds what this round needs a decision on: the clauses the vendor edited, plus the asks they left unanswered. Every row says what happened to it.

<Frame caption="The round 2 queue. Each row carries its round and what the vendor did to that clause.">
  <img src="/images/user-guides/round-queue.png" alt="Clause list where each row shows a round 2 chip and a state such as changed not asked, countered, not addressed, or accepted" />
</Frame>

Use **Review everything** if you would rather walk all the clauses, the same way you can in round 1.

## Step 4: Decide on a counter

When you open a clause the vendor countered, the middle pane compares **what you asked for** against **what came back**, rather than making you re-read the whole clause. The changed language is highlighted, and Coverbase says in one sentence why it counts the reply as a counter rather than an acceptance.

<Frame caption="A counter. Your round 1 ask above, the vendor's returned language below with the change highlighted, and the reason it is short of your standard.">
  <img src="/images/user-guides/round-reading-a-counter.png" alt="Clause comparison showing the round 1 ask, the vendor's returned language with the new sentence highlighted, and an explanation of why it is a counter" />
</Frame>

Your four verdicts are unchanged from round 1: accept as-is, flag for the vendor, accept the risk, or not applicable. **Save** records the verdict; **Skip for now** moves on without recording one.

## Step 5: Send the next round

The follow-up drafted at the end of a round carries only what is still open.

- An ask the vendor **accepted** is settled and never re-raised.
- An ask they **countered** comes back as a fresh ask about their language, not a repeat of yours.
- An ask they **did not address** is restated.
- A clause they **changed without being asked** becomes a new ask, if you flagged it.

Re-sending an ask the vendor already agreed to is how a negotiation loses credibility, so the packet will not do it.

<Note>
  Coverbase proposes which of your asks were accepted; it does not settle them quietly. When the reading is not clear-cut, the ask is left for you to confirm on the round summary, because marking an ask accepted removes it from every future message.
</Note>

## What to check on every round

<Steps>
  <Step title="Read the unasked changes first">
    Every clause in that bucket, even the ones you accepted last time.
  </Step>
  <Step title="Confirm the replaces-versus-adds reading">
    One wrong call there makes the whole round report on superseded language.
  </Step>
  <Step title="Confirm the proposed acceptances">
    Anything Coverbase was unsure about is waiting for your tick on the summary.
  </Step>
  <Step title="Look at what was not addressed">
    Silence is an answer. Decide whether to press it again or let it go, rather than sending the same sentence a third time.
  </Step>
</Steps>

## Related guides

<CardGroup cols={2}>
  <Card title="Contract Guardian" icon="file-contract" href="/user-guides/contract-guardian">
    Build a clause set, run the first review, decide each flagged clause, and send the redline.
  </Card>
  <Card title="Contract components" icon="puzzle-piece" href="/user-guides/contract-components">
    How Coverbase decides which document carries which part of an agreement, and what happens when a new document supersedes an old one.
  </Card>
</CardGroup>
